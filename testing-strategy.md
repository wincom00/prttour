# 테스트 전략 — prttour_myprt (푸른투어 ERP)

작성일: 2026-08-24
대상: `D:\www\prttour_myprt` (푸른투어 예약/ERP 시스템)

## 1. 현황 진단

이 프로젝트는 일반적인 테스트 피라미드를 그대로 적용하기 어려운 구조다. 전제부터 정리한다.

- **자동화 테스트가 전혀 없다.** `CLAUDE.md`/`AGENTS.md`에 명시된 대로 현재 검증 방식은 "수정 후 브라우저에서 직접 확인"이 전부다.
- **절차형 PHP + `mysql_*` 레거시 API.** DB 접근, 비즈니스 로직, HTML 출력이 한 파일 안에 강하게 결합되어 있어 순수 단위 테스트로 분리하기 어렵다.
- **`extract($_GET)`/`extract($_POST)` 전역 변수 주입 패턴.** 함수 경계가 흐릿해서 입력·출력을 격리하기 어렵다.
- **거대 단일 파일.** `base_reservation_m.php`가 262KB — 예약 로직의 핵심이자 가장 위험한 지점이다.
- **운영 DB 직결.** `include/dbconn.php`가 운영 DB(`98.91.65.48`, `prtadmindb`)를 가리킨다. 테스트가 이 DB를 직접 건드리면 안 된다.
- **`mysql_*` → PDO 전환 금지, 대규모 리팩토링 금지**가 `AGENTS.md`에 명문화되어 있다. 테스트 인프라를 이유로 이 원칙을 깨면 안 된다.

결론: "커버리지 80% 달성" 같은 목표는 이 코드베이스에 맞지 않는다. **회귀(regression)를 잡아내는 안전망을 위험 영역부터 얇게 깔아나가는 것**이 현실적인 전략이다.

## 2. 접근 방식 — 레거시 코드 우선순위

새 프레임워크를 도입하거나 구조를 바꾸지 않고, 기존 코드를 건드리지 않는 선에서 세 층을 쌓는다.

### Phase 1 — 브라우저 E2E 스모크 테스트 (최우선, 즉시 착수 가능)
코드를 한 줄도 바꾸지 않고 도입 가능하다. Playwright로 핵심 플로우가 화면상 깨지지 않았는지 확인한다.

- 로그인 → 예약 등록 → 견적/인보이스 생성 → 정산 반영까지 하나의 시나리오로 연결
- 매 배포/수정 전후로 실행해 "겉보기 회귀"를 잡는다
- 실제 운영 DB가 아니라 **별도 테스트 DB 스키마**(운영 DB를 복제한 스냅샷)를 가리키도록 로컬 `dbconn.php`를 분리해서 실행

### Phase 2 — AJAX 엔드포인트 계약 테스트
`get_*.php` 패턴은 입력→JSON 출력이 명확해서 테스트하기 쉬운 지점이다.

- HTTP 요청을 직접 던져서 응답 JSON 스키마/필드를 검증 (PHPUnit + Guzzle, 또는 Postman/Newman)
- 예: `get_bal.php`, `get_rev.php`, `get_pgrev.php` 등 — 파라미터별 정상/예외 케이스

### Phase 3 — 순수 함수 단위 테스트 (선택적, 여유 있을 때)
`include/c_misc_inc.php`, `include/func_list.php` 안에서 **부작용 없는(입력→출력만 있는) 함수**만 골라서 PHPUnit으로 테스트한다. DB나 세션에 의존하는 함수는 대상에서 제외 — 억지로 격리하려다 구조를 바꾸는 순간 `AGENTS.md` 원칙과 충돌한다.

## 3. 우선순위 모듈 (위험도 기준)

| 순위 | 모듈 | 대표 파일 | 이유 |
|---|---|---|---|
| 1 | 예약 | `base_reservation_m.php`, `inc_insert_reserve*.php` | 매출 직결, 최대 단일 파일 |
| 2 | 정산/결제 | `paysettle.php`, `payment_history.php`, `guide_settle.php`, `hotel_settle.php`, `car_settle_*.sql` | 금액 오류는 곧바로 손실 |
| 3 | 인보이스/견적 | `invoice_*.php`, `estimate_*.php` | 고객 대면 문서, 금액 표기 오류 민감 |
| 4 | 배정 (가이드/차량/호텔) | `guide_assign*.php`, `car_assign_m.php`, `hotel_assign_m.php` | 운영 정합성, 이중 배정 방지 |
| 5 | 인증/권한 | `login.php`, `include/func_list.php` | 계정 잠금(3회 실패), 비밀번호 26일 만료 로직 |

## 4. 커버리지 목표 (재정의)

퍼센트 기반 목표 대신 **"핵심 플로우 회귀 테스트 개수"**로 추적한다.

- 0~1개월: 1번(예약) 플로우 스모크 테스트 1개 확보
- 1~3개월: 2·3번(정산/인보이스) 플로우까지 스모크 테스트 확장, 총 3~5개 시나리오
- 이후: **새로 손댄 파일에 한해서만** 관련 스모크 테스트를 추가하는 룰(보이스카웃 룰)로 전환. 전체 레거시 파일에 소급 적용하지 않는다.

## 5. 도구 제안

| 용도 | 도구 | 비고 |
|---|---|---|
| 브라우저 E2E | Playwright | 별도 설치 필요, 로컬 Laragon 서버 대상 |
| API 계약 테스트 | PHPUnit 9.x (require-dev) 또는 Postman/Newman | `composer.json`이 비어있으므로 dev 전용으로만 도입, 운영 배포에 영향 없음 |
| 테스트 DB | 운영 DB 스냅샷 별도 스키마 | `dbconn.php`는 `.gitignore` 대상 — 테스트용 `dbconn.test.php` 별도 관리 |

## 6. 예시 테스트 케이스

- **로그인**: 정상 로그인 성공, 비밀번호 3회 오류 시 계정 잠금, 26일 경과 시 비밀번호 만료 강제
- **예약 생성**: 신규 예약 저장 후 `base_reservation_m.php` 목록에 정확히 반영되는지
- **정산 금액 일치**: 예약 → 인보이스 → 정산까지 금액이 각 단계에서 변형 없이 일치하는지 (`paysettle.php` ↔ `invoice_m.php`)
- **AJAX 엔드포인트**: `get_bal.php`에 잘못된 파라미터를 보냈을 때 500 에러 대신 정상적인 에러 JSON을 반환하는지
- **권한 체크**: 권한 없는 계정으로 정산 메뉴 접근 시 `func_list.php` 권한 함수가 차단하는지

## 7. 하지 말아야 할 것

- 테스트 도입을 명목으로 `mysql_*` → PDO/MySQLi 전환 시도 금지 (`AGENTS.md` 3.1)
- 테스트하기 쉽게 만든다고 `extract()` 패턴 제거하거나 파일을 클래스로 재구성하지 말 것 (`AGENTS.md` 4번 금지 사항)
- 테스트가 운영 DB(`98.91.65.48`)에 쓰기 작업을 하지 않도록 반드시 분리
- 커버리지 %를 KPI로 강제하지 말 것 — 이 코드베이스에서는 의미 없는 지표

## 8. 다음 단계 제안

1. Playwright로 "로그인 → 예약 등록" 스모크 테스트 1개부터 작성 (가장 빠르게 안전망 확보)
2. 테스트 전용 DB 스키마 분리 (운영 DB 복제)
3. 정산 관련 스모크 테스트 확장
4. 이후 신규 작업 시에만 관련 테스트 추가하는 룰 적용
